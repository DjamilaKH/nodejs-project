{% extends 'layout.twig' %}

{% block body %}
	<a href="/user/addform">Ajouter</a>
	<table border="1">
		<thead>

			<th>Password</th>
			<th>Email</th>
			<th>FullName</th>
			<th>UserName</th>
			<th>Action</th>
		</thead>
		<tbody>
			{% for item in data %}
				<tr>

					<td>
						{{ item.password }}
					</td>
					<td>
						{{ item.email }}
					</td>
					<td>
						{{ item.fullname }}
					</td>
					<td>
						{{ item.username}}
					</td>
					<td>
						<a href="/user/delete/{{ item._id }}">Remove</a>

						<a href="/user/updateForm/{{ item._id }}">Update</a>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
